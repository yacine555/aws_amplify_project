type Todo @model @auth(rules: [{allow: owner, ownerField: "owner"}])
{
  id: ID! @primaryKey
  content: String
  done: Boolean
  priority: Priority
  owner: String
  createdAt: AWSDateTime!
  updatedAt: AWSDateTime!
}

enum Priority {
  low
  medium
  high
}